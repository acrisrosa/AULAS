# instalação do firebase
npm install firebase

# instalação do firetools
npm install -g firebase-tools

# executar ng build (criar diretório dist/"nome_projeto"/browser)
ng build

# Entrar no firebase
firebase login

# iniciar o firebase
firebase init

# executar ng build (criar tudo até aqui)
ng build

# deploy do firebase
firebase deploy